import { AlphabetNumberingPipe } from './alphabet-numbering.pipe';

describe('AlphabetNumberingPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabetNumberingPipe();
    expect(pipe).toBeTruthy();
  });
});
