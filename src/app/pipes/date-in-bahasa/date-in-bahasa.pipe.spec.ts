import { DateInBahasaPipe } from './date-in-bahasa.pipe';

describe('DateInBahasaPipe', () => {
  it('create an instance', () => {
    const pipe = new DateInBahasaPipe();
    expect(pipe).toBeTruthy();
  });
});
